1) Run gag.console.py
2) readgff, readfasta, readtrinotate
3) settbl2asnexecutable, addtemplatefile
4) removeseq scaffold04061 -- this seq contains N's but has no features on it.
5) trimregion scaffold02211 5499 5499 -- this seq contains a terminal N #TODO TEST THIS
6) removeallgenesegments BDOR_001261 BDOR_005278 -- these genes contain other genes (??)
7) invalidateregion scaffold00001 714210 714404 -- tail end of a CDS that is all N's...
8) invalidateregion scaffold00097 602817 603018 -- (same)
9) convert NCBI beginendgap file into list of problem gene ids -- command (bash):

    grep BDOR beginendgap | sed 's/.*BDOR/BDOR/' | sort | uniq > beginendgap.gene_ids

10) cat beginendgap.gene_ids | checkgeneforinvalidbeginorend -- and get an error ... TODO... shit.

11) TODO from NCBI error reports, address all regions from 'contaminationscreen' (probably use invalidateregion)
12) ducttape -- this does 'rename maker mrnas', 'remove first cds segment if shorter than 3', 'create starts and stops' and 'remove mrnas with cds shorter than 150'. ## note: must run this AFTER invalidate region! because create start stops.


**Check .tbl and .sqn files for presence of gene_ids 'ECE2' and 'TCPG' -- they came up in error report as containing other genes, but I can't find them in any of the input files, so wtf?
